[project]
name = "fus-process"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "pynrrd>=1.1",
    "matplotlib>=3.10",
    "tqdm>=4.67",
    "openpyxl>=3.1",
    "scikit-image>=0.26",
    "requests>=2.32",
    "iconeus-scan",
]

[tool.uv.sources]
iconeus-scan = { git = "https://github.com/yuanxun-yx/iconeus-scan", tag = "v0.1.0" }
